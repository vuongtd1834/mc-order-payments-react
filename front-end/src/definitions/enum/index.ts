export enum REQUEST_STATUS {
    IDLE = "idle",
    LOADING = "loading",
    COMPLETE = "complete",
    ERROR = "error",
}

export enum ORDER_STATUS {
    CREATED,
    CONFIRMED,
    DELIVERED,
    CANCELLED,
}
